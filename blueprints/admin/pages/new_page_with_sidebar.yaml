form:
  validation: loose
  fields:
    section:
        type: section
        title: Add Page with Sidebar

    title:
      type: text
      label: Page Title
      validate:
        required: true

    folder:
      type: hidden
      default: '@slugify-title'

    route:
      type: hidden
      default: /

    name:
      type: hidden
      default: 'sidebarpage'

    visible:
      type: hidden
      default: ''

    blueprint:
      type: blueprint
